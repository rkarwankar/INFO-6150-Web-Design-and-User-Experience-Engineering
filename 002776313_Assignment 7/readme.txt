CSS Grid: Used to structure and align the first page/index page components.
CSS Flexbox: Used to structure and align the second page components.

Variables: $background-color: white; $font-stack: Serif; $dark-color: grey; 
-used for font stack and colors on both the pages.

Custom Properties:  display: backbutton(dsp);
                    background-color: backbutton(backgroundcolor);
                    border: backbutton(bdr);
                    color: backbutton(clr);
                    padding: backbutton(pddng);
                    text-align: backbutton(txt-align);
                    text-decoration: backbutton(txt-decoration);
                    font-size: backbutton(fnt-size);
                    cursor: backbutton(crsor);
                    border-radius: backbutton( brder-radius);
                    margin-left: backbutton(mrgin-left);
                    margin-right: backbutton( mrgin-right);

used for back button on the second page.

Nesting: Used on first page to style classes like 'nav,'hero' and 'footer' which are child classes for parent class 'wrapper'.

Interpolation: Used to create custom properties for back button on the second page.

Placeholder Selectors: Used in styling Show more buttons on the first page. Used in inheritance for styling buttons.

Mixins: Used in styling button on the secong page and create custom properties for buttons. 

Functions: Used to change color automatically with respect to dark and light backgrounds. Used to create custom properties.


